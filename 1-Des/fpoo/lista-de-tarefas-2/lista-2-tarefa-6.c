
#include <stdio.h>
int main (){
	int a, b, c, d, e, f;
	printf("digie seis numeros inteiros:");
	scanf("%d %d %d %d %d %d", &a, &b, &c, &d, &e, &f);
	if(a < b){		
	}else if(b >= c && c >= d && d >= e && e >= f){
		printf("%d  o maior numero dos seis" , a);
}
	return 0;
}
